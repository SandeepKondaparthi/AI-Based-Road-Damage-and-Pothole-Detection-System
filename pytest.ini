[pytest]
testpaths = tests/selenium
addopts = -ra
markers =
    smoke: Basic availability checks (ISO/IEC 25010 - reliability)
    e2e: End-to-end functional flows (ISO/IEC/IEEE 29119)
    performance: Basic client-side performance checks
    usability: Basic UI/UX validation
